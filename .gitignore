# upstream clone (reproducible via scripts/fetch_upstream.sh)
committers.top/

# crawl store: shards are committed (small per-file diffs); legacy/simulated/lock are not
data/graph/nodes*.jsonl*
data/graph/crawler.lock

# generated JSON (reproducible via scripts/convert.py)
site/src/data/
site/public/data/

# astro
site/node_modules/
site/dist/
site/.astro/
